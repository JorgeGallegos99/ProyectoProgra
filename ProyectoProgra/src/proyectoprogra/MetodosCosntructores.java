package proyectoprogra;

/*
setTitle(""); //pone el titulo
setLocationRelativeTo(true); //abre la ventana en el centro de la patalla
setLocation(x,y) posicion 
setBounds(int x, int y, ancho, alto); mover los objetos

etiquetas de texto
objeto.setForeground(Color.color)
objeto.setOpaque(true);  objeto.setBackground(Color.yellow);//color del fondo etiqueta

*/